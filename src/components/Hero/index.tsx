import React from "react";

import { IMovie } from "../../types";

interface HeroProps {
  movie?: IMovie;
}

const Hero: React.FC<HeroProps> = ({ movie }) => {
  return null;
};

export default Hero;
